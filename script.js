// DOM Elemente
document.addEventListener('DOMContentLoaded', () => {
    // Hier kommt später die Hauptlogik
});

// Hilfsfunktionen
function animate(element) {
    element.classList.add('animate-fade-in');
} 